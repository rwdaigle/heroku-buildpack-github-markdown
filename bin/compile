#!/bin/sh

FILES=*.md
for f in \$FILES
do
  echo -n "-----> Sending $f to GitHub for rendering..."
  curl -d @$f -H "Content-Type:text/plain" -s https://api.github.com/markdown/raw > $f.html
  rename s/.md// $f.html
  echo "done"
done